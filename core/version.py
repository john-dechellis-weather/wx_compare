"""One line that says which code is running.

Shown on the Home page and in every diagnostics box, so "did my
upload deploy" is answered by reading it rather than by hunting for
a feature. Bump BUILD on every batch of uploads.
"""

BUILD = "2026-09-07 r12 — chart glyphs, scope default, jet-route panel, Class B filter"
