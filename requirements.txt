streamlit>=1.61.0
starlette==1.3.1
pydeck>=0.9
requests>=2.31

# MRMS radar mosaic (core/mrms.py) and the CAM forecast overlay
# (core/cam_overlay.py + core/hrrr_cam.py). matplotlib is a fallback
# renderer only; cartopy is NOT needed.
numpy>=1.24
scipy>=1.11
Pillow>=10.0
matplotlib>=3.7
xarray>=2024.1
cfgrib>=0.9.10
eccodes>=1.6

# Single-site Level II (core/l2.py): MetPy's Level2File is the decoder.
# Pure Python, super-res aware, no pyart.
metpy>=1.6
